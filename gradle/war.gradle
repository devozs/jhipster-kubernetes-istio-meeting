apply plugin: "war"

bootWar {
    mainClassName = "ro.trc.office.meeting.MeetingApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
